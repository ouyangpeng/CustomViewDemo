1. 补间(视图)动画
    动画执行, 视图的布局并没有发生改变, 只是不断改变了绘制产生的效果
    问题: 事件响应区域总是在初始区域
2. 属性动画
    动画执行是通过不断改变视图的相关属性来实现动画效果的
    事件响应区域会随着视图的变化而变化

3. 属性动画是在3.0版本才新增的
   如果应用想用属性动画, 但需要兼容到2.x的版本手机,可以使用第三方动画库(NineOldAndroids)
	